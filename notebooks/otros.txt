#def cast(df, features_type):
#    """
    
#    """
#    df_cast = df.copy()
    
#    for x in features_type['qualitative']:
#        df_cast[x] = df_cast[x].astype(str)
    
#    for x in features_type['quantitative']:
#        df_cast[x] = df_cast[x].astype(str)
    
#    return df_cast